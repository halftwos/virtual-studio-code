<Window x:Class="_11.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="登录" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="用户名:" VerticalAlignment="Center" Margin="0,10,10,10"/>
        <TextBox Name="UsernameTextBox" Grid.Column="1" Width="200" Margin="0,10,0,10"/>

        <TextBlock Text="密码:" VerticalAlignment="Center" Grid.Row="1" Margin="0,10,10,10"/>
        <PasswordBox Name="PasswordBox" Grid.Row="1" Grid.Column="1" Width="200" Margin="0,10,0,10"/>

        <Button Content="登录" Grid.Row="2" Grid.ColumnSpan="2" Width="100" HorizontalAlignment="Center" Margin="0,10,0,0" Click="LoginButton_Click"/>
    </Grid>
</Window>


