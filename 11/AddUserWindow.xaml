<Window x:Class="_11.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加管理员" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 用户名 -->
        <Label Content="用户名:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="0" Grid.Column="1" Margin="10,5,0,10" Width="200" HorizontalAlignment="Left"/>

        <!-- 密码 -->
        <Label Content="密码:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="10,5,0,10" Width="200" HorizontalAlignment="Left"/>

        <!-- 添加用户按钮 -->
        <Button Content="添加用户" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Click="AddUserButton_Click" Width="100" Margin="0,5,0,10" HorizontalAlignment="Center"/>
    </Grid>
</Window>


