<Window x:Class="_11.ShipProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShipProductWindow" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 产品名字 -->
        <TextBlock Text="产品名字:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox x:Name="ProductNameTextBox" Grid.Row="0" Grid.Column="1" Margin="0,5,0,10" Width="200" HorizontalAlignment="Left" VerticalAlignment="Center"/>

        <!-- 出货数量 -->
        <TextBlock Text="出货数量:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox x:Name="ShipQuantityTextBox" Grid.Row="1" Grid.Column="1" Margin="0,5,0,10" Width="200" HorizontalAlignment="Left" VerticalAlignment="Center"/>

        <!-- 出货按钮 -->
        <Button Content="出货" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Click="ShipButton_Click" Width="100" Margin="0,5,0,10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>

